# Collectors (public web). Python 3.11+.
requests>=2.31
beautifulsoup4>=4.12
jsonschema>=4.20
openpyxl>=3.1        # RPI history sheets (XLSX export keeps computed values; CSV does not)

# Social scout (phase 2) - also run `playwright install chromium` once.
playwright>=1.45
